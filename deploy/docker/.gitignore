# Deployment secrets and local overrides.
.env
docker-compose.override.yml

# Scratch files, including redirected SSH host keys.
tmp/

# Persistent service data lives beside Compose but never in Git.
data/
